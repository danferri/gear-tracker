services:
  database:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: geartracker
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: app
      MYSQL_PASSWORD: app
    ports:
      - "3307:3306"